# =============================================================================
# Flux GitRepository — source for the ai-triage-nurse repo
# TODO: Replace mgodfre3 with your GitHub org or username
# =============================================================================
apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: ai-triage-nurse
  namespace: flux-system
spec:
  interval: 1m
  url: https://github.com/mgodfre3/ai-triage-nurse
  ref:
    branch: main
  secretRef:
    name: github-token
